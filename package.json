{
  "name": "cap-progressive-microservice",
  "version": "0.0.1",
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "test": "jest --passWithNoTests",
    "lint": "eslint srv/**/*.js --fix",
    "release": "npx standard-version --no-verify && git push --follow-tags origin main",
    "coverage": "npm run test -- --coverage",
    "format": "npx prettier -w src"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/VirtualOctopus/VirtualOctopusJS.git"
  },
  "engines": {
    "node": ">=18",
    "npm": ">=8"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.0.0",
    "@sap/cds-dk": "^9.0.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.0.0",
    "eslint": "^8.44.0",
    "jest": "^30.0.0",
    "mbt": "^1.2.26",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "@sap/cds": "^8.0.0",
    "glob": "^11.0.0"
  }
}
